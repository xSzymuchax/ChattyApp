const authSerice = require("../../../src/modules/auth/auth.service");

describe("AuthService", () => {
    describe("Should create credentials for new user", () => {

    });

    describe("Should not create Credentials for weak password", () => {

    });

    describe("Should not create Credentials if password and confirmation are diffrent", () => {

    });

    describe("Should not create Credentials if any data is missing", () => {

    });

    
});