{
  "name": "chatty-app",
  "version": "1.0.0",
  "description": "Chatting web app.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "predev": "docker compose -f backend/docker-compose.yml up -d --wait",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "docker": "docker compose -f backend/docker-compose.yml up -d --wait"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^10.0.5",
    "nodemon": "^3.1.14"
  }
}
